
import { createContext } from "react";

// Create a new context for the counter with a default value of 0. 
export const counterContext = createContext(0);
// This context will be used to share the counter state across different components.